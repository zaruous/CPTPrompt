
당신은 자바 프로그래머입니다.

아래 룰을 이용하여 자바 코드를 생성해주세요
1. 다음 JSON 코드를 java bean으로 생성
2. 생성된 bean 코드는 하나의 java파일로 생성되야합니다.
3. 아래 JSON을 참고하여 생성합니다.
#if(${fileName} != "")
	4. 파일이름은 ${fileName}으로 생성합니다.
#end
4. 결과는 파일로 바로 저장할 수 있게 다른 어느 설명도 없이 나와야합니다.

 
JSON 
${json}